# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IDE / Editor — lokal, enthalten Pfade dieses Rechners
*.iml
*.ipr
*.iws
.idea/
.vscode/
*.code-workspace
.claude/settings.local.json

# Flutter/Dart/Pub related
**/doc/api/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication / obfuscation
app.*.symbols
app.*.map.json

# Secrets & credentials — niemals committen
.env
.env.*
# ⚠️ Ausnahme: die Vorlage enthält KEINE Werte, nur die Namen der Schlüssel
# und die Erklärung dazu (فاز S / S.3). Ohne diese Zeile fiele sie unter
# `.env.*` und wäre unsichtbar.
!.env.example
!.env.example
*.pem
*.key
*.p8
*.p12
*.pfx
*.jks
*.keystore
key.properties
local.properties
google-services.json
GoogleService-Info.plist

# V.2 (فاز V): Wortindex — entsteht beim Bauen (tool/vokab_index.dart),
# damit er nie veralten kann. Nie von Hand anlegen oder committen.
/assets/vocab_index.json
